# auxi/maestro/flows/auth/login.yaml
# Login persists across cold relaunch (Keychain).

appId: com.auxi2026.app
name: auth-login
tags:
  - auth
  - regression
env:
  HOME_TIMEOUT_MS: 30000
  LOGIN_SCREEN_TIMEOUT_MS: 10000
---
# 1. Cold login from a clean state
- runFlow: ../_shared/login.yaml

# 2. Kill the app and relaunch — token in Keychain should still be valid
- stopApp
- launchApp:
    clearState: false
- extendedWaitUntil:
    visible:
      id: home-screen-root
    timeout: ${HOME_TIMEOUT_MS}
- assertNotVisible:
    id: auth-email-input
